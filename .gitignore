__pycache__/
.venv/
*.pyc

analysis/_ne_*_countries.gpkg

# generated plots / images (kept locally, not committed)
outputs/

# local data cache for the PNG slides (pages/png-enso/ holds the committed outputs)
analysis/_png_enso_cache/

# local credentials
.env
pipeline/.checkpoint_adm2/
pipeline/.checkpoint_uga/

# Rendered Quarto output — the tracked copy lives at pages/uganda/index.html
analysis/uganda_hnrp.html
analysis/uganda_hnrp_files/

# ENSO slides live on the prod blob (pipeline/sync_enso_slides.py); the deploy downloads them
pages/enso/slides/
pages/enso/countries.json
